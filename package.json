{
  "name": "testJS",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html -d build --public-url /JS-Dice",
    "deploy": "gh-pages -d build",
    "clean": "rm -rf dist build .cache",
    "check": "eslint --fix src/js/index.js && stylelint --fix src/css/*.css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^4.1.10",
    "eslint": "^7.10.0",
    "gh-pages": "^3.1.0",
    "stylelint": "^13.7.2",
    "stylelint-config-standard": "^20.0.0"
  }
}
